# packages/shared

Optional shared types, schemas, and utils used by more than one track.

Keep this package thin until two tracks actually need the same module. Prefer duplicating lightly over premature abstraction.
